name: omnigisto_pkg
description: "A lightweight, fast, and memory-efficient Flutter/Dart library for reading Aperio SVS histological image metadata, extracting tiles, and associated images."
version: 0.1.0-beta
homepage: https://github.com/vvsaradjev/omnigisto_pkg
repository: https://github.com/vvsaradjev/omnigisto_pkg
issue_tracker: https://github.com/vvsaradjev/omnigisto_pkg/issues


environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^4.9.2
  jpeg2000: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:


